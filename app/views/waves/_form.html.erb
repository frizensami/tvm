<%= form_for(@wafe) do |f| %>
  <% if @wafe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@wafe.errors.count, "error") %> prohibited this wafe from being saved:</h2>

      <ul>
      <% @wafe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :wave_number %><br>
    <%= f.number_field :wave_number %>
  </div>
  <div class="field">
    <%= f.label :start_time %><br>
    <%= f.datetime_select :start_time %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
